# Historical GitHub Workflows

Updated: 2026-07-24
Maintainer: Codex

This directory preserves retired GitHub Actions definitions required by release
contracts, focused verifiers, tests, and audit evidence.

The files are not active workflows because GitHub Actions only discovers
workflow definitions directly under `.github/workflows/`.

Archived definitions:

- `release-v0.16.0-manual-testnet-validation.yml`
- `release-v0.17.0-manual-artifact-validation.yml`

Do not add current runtime capability here. Current CI and operator workflows
are indexed in `.github/workflows/README.md`.
